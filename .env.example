# Optional build-time label for hosts that surface Vite env metadata.
# Vite exposes only variables prefixed with VITE_ to browser code.
VITE_APP_NAME=alex-1883-test-41

# This app is a static browser calculator. It does not require runtime
# database, auth, object-storage, or server-side API environment variables.
